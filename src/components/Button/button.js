// import PropTypes from "prop-types";
// import "./button.scss";
// export function Button({ type, text, onClick }) {
//   return (
//     <button className={"btn " + type} onClick={(e) => onClick()}>
//       {text}
//     </button>
//   );
// }

// Button.propTypes = {
//   text: PropTypes.string.isRequired,
//   onClick: PropTypes.func.isRequired,
// };
